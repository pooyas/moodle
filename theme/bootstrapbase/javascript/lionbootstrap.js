// We need to actually use the code manually here as this is tricky do in
// themes at present.
YUI().use('lion-theme_bootstrapbase-bootstrap', function(Y) {
    Y.Lion.theme_bootstrapbase.bootstrap.init();
});
